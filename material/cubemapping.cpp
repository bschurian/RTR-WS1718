#include "material/cubemapping.h"


/*void CubeMappingMaterial::apply(unsigned int light_pass)
{
    prog_->bind();

    // qDebug() << "applying Planet material";

    // first set all the uniforms for Phong general material
    Material::apply(light_pass);
    prog_->setUniformValue("skyTexture", 1);
    skyTexture->bind(1);

}*/



